export enum ErrorMessage {
  ALREADY_EXISTS = "Object already exists.",
  NOT_FOUND = "Target not found.",
  WRONG_PASSWORD = "Please check the password again.",
}

export enum ErrorStatus {
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
}
